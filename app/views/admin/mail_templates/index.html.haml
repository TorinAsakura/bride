#mails_catalog.span3
  %h3 Системные письма
  %h3 Рассылки
  %p= link_to '+ Новая рассылка', new_admin_mail_template_path, remote: true, class: 'link-style'
  .search-field
    = text_field_tag :search_field, '', placeholder: 'Введите первых 3 символа', id: 'sv-admin-search-field'
  .sv-admin-search-list
    - @mail_templates.each do |m_t|
      .mail-template--line
        = link_to m_t.title, admin_mail_template_path(m_t), remote: true, class: "sv-mail-template-#{m_t.id}"
        = link_to 'edit', edit_admin_mail_template_path(m_t), remote: true
#sv-admin-edit-mail-template
